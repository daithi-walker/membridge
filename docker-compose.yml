services:
  claude-ui:
    build: .
    image: claude-ui:latest
    container_name: claude-ui
    restart: unless-stopped
    ports:
      - "127.0.0.1:7842:7842"
    volumes:
      - "${HOME}/.claude-ui:/data"
    environment:
      CLAUDE_UI_DB: /data/sessions.db
